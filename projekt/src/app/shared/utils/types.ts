export type RadioGroupOptions = {
  id: number;
  value: string;
  label: string;
};

export type ContentData = {
  id: number;
  body: string;
};

export type InitailState = {
  selectedOption: string;
  defaultText: string;
  name: string;
};
